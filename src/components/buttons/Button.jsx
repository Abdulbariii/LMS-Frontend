const Button = () => {
  return (
    <button
      color="primary"
      onClick={() => {
        // Handle the button click event here
      }}
    >
      Action
    </button>
  );
};

export default Button;
